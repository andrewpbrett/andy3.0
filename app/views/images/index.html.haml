#images
	- @images.each_with_index do |image, i|
		= render :partial => "show", :locals => { :image => image, :size => :thumbnail }
		- if i % 6 == 5
			.clear
	.clear
	:javascript
		$(window).load(function(){
			selectPhotosNav();
			alignImages();
		});