= form_for @image, :as => :image, :url => { :action => :create }, :html => {:multipart => true} do |f|
	= f.check_box :photos
	= f.label :photos
	= f.label :permalink
	= f.text_field :permalink
	= f.file_field :image
	= f.submit